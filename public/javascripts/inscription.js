/**
 * Created by Gally on 06/02/2017.
 */
/*

function VerifMail()
{
    a = document.Verif.Mail.value;
    valide1 = false;
    for(var j=1;j<(a.length);j  ) {
        if(a.charAt(j)=='@') {
            if(j<(a.length-4)) {
                for(var k=j;k<(a.length-2);k  ) {
                    if(a.charAt(k)=='.') valide1=true;
                }
            }
        }
    }

    if(valide1==false) {
        alert("Veuillez saisir une adresse email valide.");
        return valide1;
    }


    function fausseInscription(choix){
        var tempPseudo,tempPassword;
        if(choix ==1){
            tempPseudo = $("#parinage").val();
            tempPassword = $("#promo").val();
        }else{
            tempPseudo = $("#parinage").val();
            tempPassword = $("#promo").val();
        }
        if (tempPseudo != "" && tempPassword != ""){
            alert("samarche");
        }
    }

    */

// $('.validerInsc').on("click", function(){ confirmInscription()});
//
// function confirmInscription(){
//     alert("samarche");
// }
